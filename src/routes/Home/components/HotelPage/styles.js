import styled from 'styled-components';
import {
  Pane,
} from 'evergreen-ui';

export const HotelInfo = styled(Pane)`
display: grid;
grid-template-columns: auto 100px 170px;
`;
export const HotelInfoList = styled(Pane)`
display:flex;
flex-direction:column;
`;


export const SectionWrapper = styled(Pane)`
margin-top:15px;
margin-bottom:15px;
`;
